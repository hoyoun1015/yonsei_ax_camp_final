# CLAUDE.md — Virtual Computational Chemistry Lab

매 세션 이 파일을 먼저 읽는다. 아래는 협의로 확정된 불변조건이다.

## 이전 프로젝트를 참조하지 않는다

`../최종연구/`, `../4주차/`의 코드·과제·조건 구조·문서를 **읽지도, 가져오지도 않는다.**
실행층을 포함해 전부 새로 만든다. 이전 설계의 가정이 새어 들어오는 것이
이 폴더를 새로 판 이유다.

## 절대 흔들면 안 되는 것

1. **정답은 LLM 판정이 아니다.** 어떤 주 지표도 LLM이 점수를 매기지 않는다.
   기전 서술·설명 품질을 LLM으로 채점하지 않는다. 정성 분석으로만 쓴다.

2. **자명한 기준선을 설계 전에 먼저 측정한다.**
   무엇을 만들기 전에, 가장 강한 값싼 대안이 얼마나 잘하는지부터 잰다.
   이전 세 프로젝트가 모두 가설에 유리한 비교 대상을 골라서 죽었다.

3. **τ는 방법오차에서 유도한다.** 파이프라인 재현성이 아니다.
   GMTKN55 참조값 대비 GFN2-xTB의 **서브셋별 실측 MAE**가 τ_subset이다.

4. **ABSTAIN 라벨은 기계적으로 유도한다.**
   `|참조 Δ| > τ_subset` → SUPPORTED / REFUTED
   `|참조 Δ| ≤ τ_subset` → ABSTAIN
   손으로 "작게 설정"하지 않는다. 평가 대상 도구(xTB)의 출력으로 라벨을 만들지 않는다.

5. **관측량은 ΔE.** GMTKN55 참조값이 전자에너지이므로 과제도 ΔE로 정의한다.
   ΔG·Boltzmann을 판정에 쓰면 참조값이 커버하지 않는 오차가 들어온다.
   Hessian은 허수진동 확인용으로만 쓴다.
   (열역학 보정을 넣으려면 그 오차를 별도로 측정해 τ에 합산하고 문서화한다.)

6. **Fixed 기준선은 최강으로 잡는다.** 약한 고정 파이프라인과 비교해서 이기는 것은
   결과가 아니다. 그리고 **규칙 기반 abstention 기준선**을 반드시 포함한다 —
   `|xTB Δ| ≤ τ_subset이면 ABSTAIN`. 이 한 줄이 하한선이자 진짜 비교 대상이다.

7. **τ와 라벨은 동결 후 불변.** 동결 시점을 문서에 남기고, 결과를 본 뒤에는
   어떤 이유로도 수정하지 않는다.

8. **과제 수는 100개 이상.** 참조값에서 자동 유도되므로 가능하고, 계산이 공짜다.
   12~20개로는 검정력이 없다.

## 주제와 기여 순서

**주제는 고정이다: "분자 시뮬레이션을 통한 자율적 화학 가설 검증 AI 연구 에이전트".**
연구 질문은 RQ1(가설 해석) · RQ2(실험 설계) · RQ3(적응적 추론) · RQ4(결론·과대해석 억제).
**이 주제 진술과 RQ 구성을 임의로 바꾸지 않는다.** 바꿔야 할 이유가 생기면
먼저 사용자에게 확인한다.

기여 순서 (제목·초록이 이 순서를 따른다)

1. **자연어 화학 가설 → 계산실험 → 결론에 이르는 자율 에이전트 구현** (RQ1~RQ4)
2. **RQ3의 채점 기준** — 에스컬레이션 판단의 객관적 정답 (SUFFICIENT/ESCALATION/FUTILE)
3. **RQ4의 채점 기준** — 과대해석을 방법오차 기준으로 판정한다.
4. tool-limited vs agent-limited 오류 분해
5. 재현 패키지

**단, 파이프라인 구현 자체를 신규성으로 주장하지 않는다.** 자연어→양자화학 계산은
이미 여러 시스템이 한다. 시스템은 만들되, **논문이 구별되는 지점은 그 판단을 무엇으로
채점하느냐(기여 2·3)** 임을 본문에서 명확히 한다.

🔲 **선행연구는 새로 조사한다.** 조사 전까지 특정 논문을 근거로 인용하지 않는다.

## 하지 않기로 한 것

- **closed loop 성능 경쟁에 들어가지 않는다.** 상충하는 결과가 보고된 영역이고
  25일 단독으로 중재할 수 없다. 적응적 선택은 **메커니즘**으로 쓰되 헤드라인
  주장으로 삼지 않는다.
- **멀티에이전트 우위를 주장하지 않는다.** 역할 분리는 기능적 필요로만 두고,
  single vs multi 비교는 부차 분석으로 내린다. 검정력이 없다.
- **효율성(계산 횟수 절감)을 주장하지 않는다.** 잡당 0.3초에서 5회 절약은
  과학적 통화가 아니다. 상위 계산(DFT)을 escalation으로 넣어 실제 비싼 자원을
  아끼는 경우에만 주장한다.

## 예상 결과에 대한 정직한 합의

가장 유력한 결과는 **규칙 기반 기준선이 에이전트를 이기는 것**이다. 그래도 논문은 성립한다.
주장의 강도를 이렇게 잡는다 — 시스템은 만들었다고 쓰되, 성능을 과장하지 않는다.

> "자연어 가설을 통제된 계산실험으로 변환하는 에이전트를 구현했다. 이 에이전트는
> 자기 방법의 오차보다 작은 차이에 대해 X%의 경우 단정적 결론을 내렸다.
> 즉 계산은 수행하지만 그 계산의 한계는 인식하지 못한다."

"우리 시스템이 최고다"도 아니고, "우리는 아무것도 안 만들고 재기만 했다"도 아니다.

## 변경 관리

호출 수, 라벨, τ 정책, QC 기준, 스키마, 모델, 프롬프트, 통계에 대한 변경은
구현 **전에** `docs/DECISION_LOG.md`에 기록한다. 동결 이후의 변경은
탐색적(exploratory)으로 표시하고 확증 결과를 대체하지 않는다.
