# CLAUDE.md — Virtual Computational Chemistry Lab

매 세션 이 파일을 먼저 읽는다. 아래는 협의로 확정된 불변조건이다.

## 이전 프로젝트를 참조하지 않는다

`../최종연구/`, `../4주차/`의 코드·과제·조건 구조·문서를 **읽지도, 가져오지도 않는다.**
실행층을 포함해 전부 새로 만든다. 이전 설계의 가정이 새어 들어오는 것이
이 폴더를 새로 판 이유다.

## 절대 흔들면 안 되는 것

1. **정답은 LLM 판정이 아니다.** 어떤 주 지표도 LLM이 점수를 매기지 않는다.
   기전 서술·설명 품질을 LLM으로 채점하지 않는다. 정성 분석으로만 쓴다.

2. **자명한 기준선을 설계 전에 먼저 측정한다.**
   무엇을 만들기 전에, 가장 강한 값싼 대안이 얼마나 잘하는지부터 잰다.
   이전 세 프로젝트가 모두 가설에 유리한 비교 대상을 골라서 죽었다.

3. **τ는 방법오차에서 유도한다.** 파이프라인 재현성이 아니다.
   GMTKN55 참조값 대비 GFN2-xTB의 **서브셋별 실측 MAE**가 τ_subset이다.

4. **ABSTAIN 라벨은 기계적으로 유도한다.**
   `|참조 Δ| > τ_subset` → SUPPORTED / REFUTED
   `|참조 Δ| ≤ τ_subset` → ABSTAIN
   손으로 "작게 설정"하지 않는다. 평가 대상 도구(xTB)의 출력으로 라벨을 만들지 않는다.

5. **관측량은 ΔE.** GMTKN55 참조값이 전자에너지이므로 과제도 ΔE로 정의한다.
   ΔG·Boltzmann을 판정에 쓰면 참조값이 커버하지 않는 오차가 들어온다.
   Hessian은 허수진동 확인용으로만 쓴다.
   (열역학 보정을 넣으려면 그 오차를 별도로 측정해 τ에 합산하고 문서화한다.)

6. **Fixed 기준선은 최강으로 잡는다.** 약한 고정 파이프라인과 비교해서 이기는 것은
   결과가 아니다. 그리고 **규칙 기반 abstention 기준선**을 반드시 포함한다 —
   `|xTB Δ| ≤ τ_subset이면 ABSTAIN`. 이 한 줄이 하한선이자 진짜 비교 대상이다.

7. **τ와 라벨은 동결 후 불변.** 동결 시점을 문서에 남기고, 결과를 본 뒤에는
   어떤 이유로도 수정하지 않는다.

8. **과제 수는 100개 이상.** 참조값에서 자동 유도되므로 가능하고, 계산이 공짜다.
   12~20개로는 검정력이 없다.

## 주제와 기여 순서

**주제는 고정이다: "분자 시뮬레이션을 통한 자율적 화학 가설 검증 AI 연구 에이전트".**
연구 질문은 RQ1(가설 해석) · RQ2(실험 설계) · RQ3(적응적 추론) · RQ4(결론·과대해석 억제).
**이 주제 진술과 RQ 구성을 임의로 바꾸지 않는다.** 바꿔야 할 이유가 생기면
먼저 사용자에게 확인한다.

기여 순서 (제목·초록이 이 순서를 따른다)

1. **자연어 화학 가설 → 계산실험 → 결론에 이르는 자율 에이전트 구현** (RQ1~RQ4)
2. **RQ3의 채점 기준** — 에스컬레이션 판단의 객관적 정답 (SUFFICIENT/ESCALATION/FUTILE)
3. **RQ4의 채점 기준** — 과대해석을 방법오차 기준으로 판정한다.
4. tool-limited vs agent-limited 오류 분해
5. 재현 패키지

## 🔒 위계 — 주인공은 에이전트다 (2026-08-11 확정)

| 순위 | 무엇 | 역할 |
|---:|---|---|
| **1** | **자율적 화학 가설 검증 AI 연구 에이전트** | **연구의 주인공** |
| 2 | 계산 도구 실행과 결과 기반 **closed loop** | 핵심 메커니즘 |
| 3 | **τ / 방법오차 인식** | 과도한 결론을 막는 scientific reasoning 장치 |
| 4 | xTB → DFT **escalation** | adaptive research decision의 **한 구현** |
| 5 | **GMTKN55 벤치마크 · 규칙 baseline** | 객관적 검증 수단 |

**τ는 연구 주제가 아니다.** *"이 계산 결과를 믿고 결론을 내려도 되는가"*를 판단하기 위한
도구이자 uncertainty mechanism이다. **escalation도 목적이 아니다** — *"결과를 보고 다음
연구 행동을 선택하는 능력"*의 한 사례다.

**따라서 이 연구를 «τ로 xTB/DFT를 라우팅하는 시스템»이나 «방법오차 기반 abstention
연구»로 좁히지 않는다.**

**신규성 주장 방식.** "계산 도구를 쓰는 에이전트를 최초로 만들었다"고 하지 않는다 —
ChemGraph·El Agente Q·AutoDFT·TritonDFT·MDAgent가 이미 하고, 랩미팅 구조는
Virtual Lab(*Nature* 2025)이 실었다. 차별점은 **그 위에서 실제 화학 가설 검증 과정의
scientific decision-making을 얼마나 제대로 수행하는지 보이는 것**이다.
**«추론·증거를 평가한 최초»라고 하지 않는다.** 다수의 화학·과학 에이전트 benchmark가
과제 성공·정답 일치·실행 성공률 중심으로 채점해 온 것은 맞지만, StatefulDiscovery
(arXiv 2606.11851)처럼 **evidential support 자체를 평가하는 최근 사례가 있다**
(`paper_logic/gap_analysis.md` §6 — 본문 직접 확인). 차별점은 **채점 축**에 있다 —
상대에너지 계산화학이라는 도메인에서 **실측 방법오차 기준의 결정론적 함수**로
증거 충분성을 판정하며 그 경로에 모델이 개입하지 않는다는 것이다.
**우리 평가가 더 낫다고 하지 않고**(도메인·지표가 다르다), **에이전트 내부는 우리도
LLM이다**(Skeptical Reviewer는 저들의 판정 skill과 같은 종류의 구성요소다).

🔒 **이 위계를 임의로 바꾸지 않는다.** 08-08·08-10에 채점(τ)이 주제로, 08-11에
escalation 성능이 헤드라인으로 올라간 일이 있었고 그때마다 사용자가 되돌렸다.
바꿔야 할 이유가 생기면 **먼저 사용자에게 확인한다.**

## 에이전트에게 반드시 남겨야 하는 결정권

이것이 없으면 fidelity router이고 위계 1번이 성립하지 않는다.

1. **가설 조작화** — 무엇을 비교하고 무엇을 관측할지 스스로 정한다
2. **비교 대상 식별** — 가설의 화학적 서술로부터 구조 쌍을 스스로 특정한다
   (자율 식별형. conformer 계열 5~10과제 파일럿으로 먼저 검증하고, 자연어만으로
   특정이 어려운 계열은 쌍 지정형으로 남겨도 된다 — 계열별 분리 보고)
3. **증거 충분성 판단** — 지금 증거로 결론을 내려도 되는가
4. **후속 행동 선택** — 상위 계산(분기 A) 또는 **조작화 재수립(분기 B)**

🔒 **분기 B의 제약 — 원 가설은 절대 바꾸지 않는다.** operationalization만 수정하고,
최종 결론은 반드시 **원 가설**에 대한 지지/기각/판단보류여야 한다. 실행층이 원 가설을
시작 시점에 고정 기록하고 결정론적으로 검사한다(Hypothesis Fidelity 지표).

## 🔒 평가 인프라는 건드리지 않는다

**τ, 224반응 331구조 계산, 비용 실측, 규칙 baseline은 완료된 평가 인프라다.**
다시 계산하지 않고, 폐기하지 않고, 그대로 위계 5번 자리에 유지한다.

🔲 **선행연구는 새로 조사한다.** 조사 전까지 특정 논문을 근거로 인용하지 않는다.

## 하지 않기로 한 것

- **closed loop 성능 경쟁에 들어가지 않는다.** 상충하는 결과가 보고된 영역이고
  25일 단독으로 중재할 수 없다. 적응적 선택은 **메커니즘**으로 쓰되 헤드라인
  주장으로 삼지 않는다.
- **멀티에이전트 우위를 주장하지 않는다.** 역할 분리는 기능적 필요로만 두고,
  single vs multi 비교는 부차 분석으로 내린다. 검정력이 없다.
- **효율성(계산 횟수 절감)을 주장하지 않는다.** 잡당 0.3초에서 5회 절약은
  과학적 통화가 아니다. 상위 계산(DFT)을 escalation으로 넣어 실제 비싼 자원을
  아끼는 경우에만 주장한다.
  > ✅ **2026-08-10, 이 조건이 충족됐다.** L1 0.02초 대 L3 25초~53분,
  > **1,000배 이상**을 실측했다(`docs/D1_실측결과.md` §7). 따라서 자원 절감은
  > 주장 가능하며, 기여 1의 헤드라인이 그것이다. 단 **절감만 단독으로 쓰지 않는다** —
  > 품질이 유지됐음을 τ로 함께 보인다.

## 예상 결과에 대한 정직한 합의

결과가 두 갈래로 갈리고, **어느 쪽으로 쓸지는 사전등록에 미리 적는다.** 결과를 보고
프레임을 정하면 체리피킹이다.

**어느 갈래든 기여 1·2는 남는다.** 시스템이 가설 검증 과정을 자율적으로 수행했다는
증거(§7.2 동작 지표 + 대표 사례 트레이스)는 규칙 기준선과의 승패에 의존하지 않는다.
**갈리는 것은 판단 품질에 대한 주장의 강도뿐이다.**

**갈래 1 — 판단 품질이 기준선을 넘는 경우.**

> "자연어 화학 가설을 자율 검증하는 에이전트를 구현했다. 이 에이전트는 가설을 스스로
> 조작화하고 비교 대상을 특정했으며, 증거가 방법오차에 묻힐 때 판단을 보류하고 필요한
> 경우 상위 계산을 선택했다. 규칙 기반 기준선보다 판단 정확도가 Y%p 높았고,
> 전량 고비용 계산 대비 자원을 X% 절감했다."

**갈래 2 — 대등한 경우.** 여전히 **가장 유력한 결과**로 보고 있다.

> "시스템은 가설 검증 과정을 자율적으로 수행했으나, 증거 충분성 판단에 관해서는
> 규칙 한 줄과 대등했다. 즉 계산은 수행하지만 그 계산의 한계 인식은 규칙 이상으로
> 나아가지 못하며, 정교한 구조가 이를 개선하지 않는다."

**갈래 3 — 재조작화 경로를 쓰지 않는 경우.** 결정권을 줬는데 안 쓰면 그 사실이 결과다
(Loop Utilization 지표). "자유도를 부여했으나 사용하지 않았다"로 보고한다.

어느 갈래든 **"우리 시스템이 최고다"도 아니고 "아무것도 안 만들고 재기만 했다"도 아니다.**

## 변경 관리

호출 수, 라벨, τ 정책, QC 기준, 스키마, 모델, 프롬프트, 통계에 대한 변경은
구현 **전에** `docs/DECISION_LOG.md`에 기록한다. 동결 이후의 변경은
탐색적(exploratory)으로 표시하고 확증 결과를 대체하지 않는다.
